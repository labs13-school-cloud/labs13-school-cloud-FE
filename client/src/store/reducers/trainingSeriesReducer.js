import {} from "../actions";

const initialState = {
  teamMembers: []
};

const trainingSeriesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default trainingSeriesReducer;
